# 文字優先與異常欄位 OCR

已依金腦版 `index.html` 與原專案 `反饋謄本` 歸納第一批規則，實裝在地政版。金腦版檔案未修改。

## 樣本與查證範圍

- 共 76 個 PDF；依 SHA-256 排除 12 個完全相同檔案後，64 個獨立檔案共 445 頁。
- 3 個檔案沒有可用文字層；不將空文字當成合法欄位样貌。
- 金腦版實際解析得到 43 份主資料及 794 筆異動。這是解析輸出樣本，不代表已人工核對每頁每欄。
- 794 筆異動的日期與登記次序均符合下列規則；權利人中有 8 筆全遮罩 `＊＊`，來源本來就不揭露姓名，保留而不猜測。
- 初步橫列擷取曾把「相關他項權利登記次序：共0筆」誤認為登記次序，並把多欄表頭當成缺值；實裝時排除這些情況。金腦原有主資料解析仍使用原本表格處理方式。

## 第一批規則

| 欄位 | 保留的合法樣貌 | 局部 OCR 的觸發條件 |
|---|---|---|
| 日期：登記、異動、原因發生、建築完成 | 民國前綴可省略，2–3 碼年、年月日完整；驗證實際曆日與閏年 | 缺值、格式破損、13 月、無效日期等 |
| 登記次序 | `0001`、`0001-000`；來源明示 `(空白)` 或無值註記保留 | 碼數錯誤、分隔符錯誤、混入文字；「相關…共幾筆」不屬於本欄 |
| 所有權人、權利人 | 遮罩姓名、全遮罩、法人及政府機關名稱；不因少見姓名或法人含數字判錯 | 分隔線、空欄、混入證號／欄名／明顯浮水印用語 |
| 面積、總面積 | 非負數值、小數與千位分隔；平方公尺等單位；允許前置補位星號 | 缺值、非數值、混入旁欄文字；不設定任意面積上限 |
| 擔保債權總金額 | 新台幣／新臺幣、數字與千位分隔、補位星號、元／仟元／萬元／億元、元整／元正 | 缺值、數字或單位破損、混欄；不設定任意金額上限 |

空白、全形空白、零寬字元、全形數字與冒號先正規化以比較；原文仍保留。合法日期後方的機關浮水印不併入日期值。多欄空表頭不直接視為缺值。

## 正式流程

1. 預設啟用「文字優先：異常欄位才用局部 OCR 修正」。先讀 PDF.js 文字；原本嵌入圖片的索引列仍由既有離線 OCR 補入，這些已辨識圖片列不再次當成原生文字重試。
2. 依頁碼、位置與欄名擷取待檢查值。通過規則即保留，該欄不呼叫 OCR；不跨欄繼承或補猜姓名。
3. 僅異常欄位送到本機局部辨識，保留少量上下文，分別辨識原圖與淡化紅色浮水印後的圖。
4. OCR 必須能找到同一欄名，結果通過規則；原圖與淡化後若產生不同的合法候選，保留原值並提示衝突。只有唯一合法候選才修正記憶體中的文字片段，再交給既有解析器。
5. 顯示檔名、頁碼、座標、原值、觸發原因、兩種 OCR、採用值與狀態。此稽核明細僅保留於本次畫面，未新增資料庫結構；原始 PDF 不改寫。

原有完整逐頁核對保留為選用功能，預設不勾選。局部辨識失敗、無法定位或沒有唯一合法候選，不自動改寫。這套規則能找出明顯格式偏誤，無法發現格式合法但內容錯誤的日期、金額或姓名；也不保證涵蓋所有表格、跨頁或地方格式。

## 驗證

- `tests/field-repair.cjs`：合法值不呼叫 OCR、曆日、補位符、全遮罩、元正、旁欄隔離、局部實際 OCR、候選衝突與服務失敗保留原值。
- `tests/field-repair-pipeline.cjs`：對 `115BE050301IDXE.pdf` 的 PDF.js 文字模擬無效月份，圖面保持原樣；經實際抽字、局部 OCR、片段修正及異動解析器，恢復 `096年11月14日` 並保留修正明細。
- `tests/compare_transcripts.cjs --verify-source`：地政版啟用預設修正模式，比對既有主資料、20 筆異動、0714 多權利人、補充索引與歷史繼承。

以上測試使用既有本機入口 `啟動地政謄本版.cmd`／8011，無外部 OCR 或公司資料傳送。
